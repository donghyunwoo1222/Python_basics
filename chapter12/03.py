# set

tags = ["Python", "AI", "Python", "Data", "AI"]

print(type(tags))
print(len(tags))


unique_tags = set(tags)

print(type(unique_tags))
print(len(unique_tags))
print(unique_tags)
0
# set을 쓰지 말고 list 기능만 써서 unique_tags를 구현하시오.
# 수도코드(의사코드)로 작성해줘.

#일단 for로 돌려. 그담에 if로 이미 있으면? 지운다? 반복. 출력.

